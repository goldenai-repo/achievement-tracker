DATABASE_URL=sqlite:///../data/achievement_tracker.db
BOUNDARY_DATA_DIR=../data/raw/boundaries
FIREBASE_PROJECT_ID=milestonetracker-506101
# Optional for local development only. Cloud Run should use Application Default Credentials.
# FIREBASE_CREDENTIALS_PATH=/secure/path/to/milestonetracker-firebase-adminsdk.json
WEB_ORIGIN=http://localhost:5173

# Production only: point this at a secret-mounted Firebase service account.
# GOOGLE_APPLICATION_CREDENTIALS=/run/secrets/firebase-service-account.json
